<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>403 - Forbidden acess </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #ffffff;
            color: #2B2D42;
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .error-container {
            text-align: center;
        }
        .error-code {
            font-size: 8rem;
            font-weight: bold;
            color: #D10024;
        }
        .error-message {
            font-size: 1.5rem;
            margin-bottom: 1rem;
        }
        .home-button {
            background-color: #D10024;
            color: #ffffff;
            border: none;
            padding: 0.75rem 1.5rem;
            font-size: 1rem;
            border-radius: 5px;
            text-decoration: none;
        }
        .home-button:hover {
            background-color: #B0001E;
            color: #ffffff;
        }
    </style>
</head>
<body>
<div class="error-container">
    <div class="error-code">403</div>
    <div class="error-title" style="font-size: 2rem; font-weight: bold; margin: 1rem 0; color: #2B2D42;">Forbidden</div>
    <p class="error-message">You don't have permission to access this resource.</p>
    <a href="{{ path('app_error') }}" class="home-button">Return to Home Page</a>
</div>
</body>
</html>