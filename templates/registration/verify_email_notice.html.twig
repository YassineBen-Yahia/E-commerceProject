<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Email Verification Notice</title>
    <link type="text/css" rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <!-- Flash Messages -->
            {% for label, messages in app.flashes %}
                <div class="alert alert-{{ label }} alert-dismissible fade show" role="alert">
                    {% for message in messages %}
                        {{ message }}
                    {% endfor %}
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            {% endfor %}

            <div class="card shadow-lg">
                <div class="card-header text-white text-center" style="background-color: #2B2D42;">
                    <h3 class="mb-0">Verify Your Email</h3>
                </div>
                <div class="card-body p-4 text-center">
                    <p class="mb-4" style="font-size: 1.2rem;">
                        A verification email has been sent to your email address. Please check your inbox and click the link to verify your email.
                    </p>
                    <p class="text-muted">
                        If you did not receive the email, please check your spam folder or <a href="{{ path('app_resend_verification_email') }}" class="text-primary">click here</a> to resend it.
                    </p>
                    <div class="d-flex justify-content-center mt-4">
                        <a href="{{ path('app_index') }}" class="btn btn-lg" style="background-color: #D10024; color: white; width: 200px;">Go to Homepage</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>