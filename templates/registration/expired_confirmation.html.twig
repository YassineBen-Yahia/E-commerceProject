<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Confirmation Expired</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #ffffff;
            color: #2B2D42;
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .card-header {
            background-color: #2B2D42;
            color: white;
        }
        .btn-primary {
            background-color: #D10024;
            border-color: #D10024;
        }
        .btn-primary:hover {
            background-color: #B0001E;
            border-color: #B0001E;
        }
        .btn-secondary {
            background-color: #2B2D42;
            border-color: #2B2D42;
            color: white;
        }
        .btn-secondary:hover {
            background-color: #1D1F2F;
            border-color: #1D1F2F;
        }
        .expired-icon {
            font-size: 4rem;
            color: #D10024;
            margin-bottom: 1rem;
        }
    </style>
</head>
<body>
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-lg mt-5">
                <div class="card-header text-center">
                    <h3 class="mb-0">Email Confirmation Expired</h3>
                </div>
                <div class="card-body text-center p-4">
                    <div class="expired-icon">
                        <i class="bi bi-clock-history"></i>⏱️
                    </div>

                    <h4 class="mb-3">Your confirmation link has expired</h4>

                    <p class="mb-4">For security reasons, email confirmation links are only valid for a limited time. Please request a new confirmation email to verify your account.</p>

                    <div class="d-grid gap-3 col-md-8 mx-auto">
                        <a href="{{ path('app_account_verify_email') }}" class="btn btn-primary btn-lg">
                            Request New Confirmation
                        </a>

                        <a href="{{ path('app_index') }}" class="btn btn-secondary btn-lg">
                            Return to Home Page
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>